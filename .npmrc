# openai@4's optional peer dep on zod@^3 conflicts with our zod@^4 (used for our own
# request validation, unrelated to their zod-based structured-output helper, which
# this project doesn't use). legacy-peer-deps avoids a spurious ERESOLVE failure.
legacy-peer-deps=true
